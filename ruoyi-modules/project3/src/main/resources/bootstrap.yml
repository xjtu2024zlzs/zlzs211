# Tomcat
server:
  port: 9303

# Spring
spring:
  application:
    name: project3
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      username: nacos
      password: nacos
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        file-extension: yml
        username: nacos
        password: nacos
  config:
    import:
      - nacos:application-dev.yml?group=DEFAULT_GROUP
      - nacos:project3-dev.yml?group=DEFAULT_GROUP