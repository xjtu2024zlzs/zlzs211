server:
  port: 9303

spring:
  application:
    name: project3
  profiles:
    active: dev
  config:
    import:
      - optional:nacos:project3-dev.yml?group=DEFAULT_GROUP
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      username: nacos
      password: nacos
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        group: DEFAULT_GROUP